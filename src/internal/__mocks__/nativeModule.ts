/**
 * @format
 */
/* eslint-env jest */

const RNCNetInfoMock = {
  configure: jest.fn(),
  getCurrentState: jest.fn(),
  addListener: jest.fn(),
  removeListeners: jest.fn(),
};

export default RNCNetInfoMock;
